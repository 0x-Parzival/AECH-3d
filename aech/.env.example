# Port for the API server
PORT=8080

# Comma-separated list of allowed CORS origins.
# Use "*" to allow all origins (suitable for local development).
# For production, specify your frontend domain(s), e.g., https://yourfrontend.com,https://another.com
CORS_ALLOWED_ORIGINS="*"
